const UnderStandChildren = ({ children }) => {
  return (
    <div
      style={{
        backgroundColor: 'white',
        borderRadius: 10,
        color: 'black',
        padding: 10,
        margin: 10,
      }}
    >
      {children}
    </div>
  );
};
export default UnderStandChildren;
